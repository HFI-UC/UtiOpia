import React from 'react';
import Write from '@/pages/Write';

// Delegate to the page component. The page itself handles conditional iOS glass wrapping.
const GlassWrite = () => <Write />;

export default GlassWrite;
