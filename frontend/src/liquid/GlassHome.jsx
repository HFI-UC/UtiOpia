import React from 'react';
import Home from '@/pages/Home';

// Keep GlassHome redirecting to Home. The per-card wrapping is handled inside Home now.
const GlassHome = () => <Home />;

export default GlassHome;
